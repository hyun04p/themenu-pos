import React from 'react';
import LandingPage from './LandingPage';

interface props {}

const WelcomeRouter: React.FC<props> = (props) => {
  return <LandingPage />;
};

export default WelcomeRouter;
